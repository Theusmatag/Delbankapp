import 'package:flutter/material.dart';

class BookReviews extends StatelessWidget {
  const BookReviews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Book Reviews')),
    );
  }
}
